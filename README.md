# Cross-city safety board (VAST Builders Challenge — Team A)

Same natural-language query across Bangkok, Dublin, and London Pack A cameras.

```sh
python3 safety-board/app.py
```

Then open http://127.0.0.1:8765. Credentials come from `/config/*.config` on the event VM (not in this repo).
